{
	"name": "cms",
	"scripts": {
		"build": "cd packages/admin && npm run build && cp dist/index.js ../../public",
		"start": "npm run build && wrangler pages dev public --live-reload"
	},
	"dependencies": {
		"@octokit/rest": "^20.0.2",
		"cookie": "^0.6.0"
	}
}
